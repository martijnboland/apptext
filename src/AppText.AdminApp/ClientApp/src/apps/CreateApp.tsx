import * as React from 'react';

interface CreateAppProps {
}

const CreateApp: React.FunctionComponent<CreateAppProps> = (props) => {
  return (
    <h1>Create app</h1>
  );
};

export default CreateApp;